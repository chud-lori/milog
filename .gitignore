# Local working doc — not merged to main.
plan.md

# Editor / OS cruft
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Test fixtures left behind by ad-hoc runs
/tmp-milog-*/

# Go build artifacts — compiled per-platform, not committed. Binary is
# distributed via GitHub Releases once Phase 6 (packaging) lands.
/go/bin/
